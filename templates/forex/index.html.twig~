{% extends 'base.html.twig' %}

{% block body %}
    <ul>
	{% for date in availableDates %}
	    <li>
		{{ date["published_at"].format('d.m.Y') }}
	    </li>
	{% endfor %}
    </ul>

    <table>
	<thead>
	    <tr>
		<th>Currency</th>
		<th>Rate</th>
	    </tr>
	</thead>
	<tbody>
	    {% for rate in forexRates %}
		<tr>
		    <th>{{ rate.currency }}</th>
		    <th>{{ rate.rate }}</th>
		</tr>
	    {% endfor %}
	</tbody>
    </table>
{% endblock %}
